name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

# Least privilege by default; the publish job widens it for itself.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          # Single source of truth: the toolchain follows go.mod.
          go-version-file: go.mod
          cache: true

      - name: Check formatting
        run: |
          unformatted="$(gofmt -l ./cmd ./internal)"
          if [ -n "$unformatted" ]; then
            echo "::error::These files need gofmt:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Test
        # -race matters here: five workers share one tracker client and one
        # SQLite handle, so data races are the failure mode worth catching.
        run: go test -race ./...

      - name: Build
        run: go build ./...

  docker:
    name: Build and push image
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Resolve image name
        id: image
        # GHCR rejects uppercase, and a repository owner may well have some.
        run: |
          echo "name=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      # arm64 emulation is only needed for the runtime stage's apk install;
      # the Go build itself cross-compiles natively (see Dockerfile).
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4

      # A pull request from a fork cannot access the registry, so only log in
      # when we are actually going to push.
      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Two published tags, nothing else:
      #   push to the default branch -> :latest, the rolling tag the NAS pulls;
      #   push of a git tag -> the version without the v, so v1.0.0 gives
      #     :1.0.0. That is what type=semver does; type=ref,event=tag would
      #     keep the leading v instead.
      # Pull requests get a tag only so the build has a name; they never push.
      - name: Derive tags and labels
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref_name == github.event.repository.default_branch }}
            type=semver,pattern={{version}}
            type=ref,event=pr

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          # Pull requests build to verify the Dockerfile, but never publish.
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # On tag builds, bake the tag into the binary (main.version, served
          # by GET /version). Branch and PR builds keep the "dev" default.
          build-args: |
            VERSION=${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'dev' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Synology's Container Manager trips over the extra attestation
          # manifests, so keep the index to plain platform entries.
          provenance: false
          sbom: false
